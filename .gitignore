/vendor
/node_modules
composer.lock
.phpunit.result.cache
.phpunit.cache/
.DS_Store
.idea
.vscode

# Local-only development artifacts — never push to public history
.kaisser.yml
BOOTSTRAP.md
CLAUDE.md
decisions.md
session-handoff.md

# Planning artifacts — tracked on homolog branch for cross-session continuity.
# Stripped from main in the homolog→main release commit before tagging public versions.
# (Not gitignored so plans persist across Claude sessions and contributors.)

# VitePress build output and cache — never commit
docs/.vitepress/dist/
docs/.vitepress/cache/

# Node modules — never commit
node_modules/
